services:
  sc-tool3-web:
    build:
      context: ../..
      dockerfile: deploy/nas/Dockerfile
    image: sc-tool3-web:nas
    container_name: sc-tool3-web
    ports:
      - '4173:4173'
    env_file:
      - ../../.env
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'wget -q -O - http://127.0.0.1:4173 >/dev/null 2>&1 || exit 1']
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    volumes:
      - ../../static/uploads:/app/static/uploads
